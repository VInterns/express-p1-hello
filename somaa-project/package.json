{
  "name": "express-app1-hello",
  "version": "1.0.0",
  "description": "A simple express app that implements a GET method to return a JSON object.",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ahmed Ismail",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.2"
  }
}
